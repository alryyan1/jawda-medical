# دليل نظام إدارة الإقامات (Hospital Admissions Management System)

## 📋 نظرة عامة

نظام إدارة الإقامات هو نظام متكامل لإدارة إقامات المرضى في المستشفى، يتضمن إدارة الأقسام (Wards)، الغرف (Rooms)، الأسرّة (Beds)، والإقامات (Admissions). النظام مبني على Laravel في Backend و React/TypeScript في Frontend.

## 🏗️ هيكل النظام

### قاعدة البيانات

النظام يتكون من 4 جداول رئيسية:

1. **wards** (الأقسام)
   - `id`: معرف فريد
   - `name`: اسم القسم
   - `description`: وصف القسم
   - `status`: حالة القسم (نشط/غير نشط)
   - `created_at`, `updated_at`: تواريخ الإنشاء والتحديث

2. **rooms** (الغرف)
   - `id`: معرف فريد
   - `ward_id`: معرف القسم (Foreign Key)
   - `room_number`: رقم الغرفة
   - `room_type`: نوع الغرفة (خاصة، شبه خاصة، عامة)
   - `capacity`: سعة الغرفة (عدد الأسرّة)
   - `status`: حالة الغرفة
   - `created_at`, `updated_at`

3. **beds** (الأسرّة)
   - `id`: معرف فريد
   - `room_id`: معرف الغرفة (Foreign Key)
   - `bed_number`: رقم السرير
   - `status`: حالة السرير (available, occupied, maintenance)
   - `created_at`, `updated_at`

4. **admissions** (الإقامات)
   - `id`: معرف فريد
   - `patient_id`: معرف المريض (Foreign Key)
   - `ward_id`: معرف القسم (Foreign Key)
   - `room_id`: معرف الغرفة (Foreign Key)
   - `bed_id`: معرف السرير (Foreign Key)
   - `admission_date`: تاريخ الإقامة
   - `admission_time`: وقت الإقامة
   - `discharge_date`: تاريخ الإخراج (nullable)
   - `discharge_time`: وقت الإخراج (nullable)
   - `admission_type`: نوع الإقامة (طوارئ، مجدولة، نقل)
   - `admission_reason`: سبب الإقامة
   - `diagnosis`: التشخيص
   - `status`: حالة الإقامة (admitted, discharged, transferred)
   - `doctor_id`: معرف الطبيب (Foreign Key, nullable)
   - `user_id`: معرف المستخدم الذي أنشأ الإقامة
   - `notes`: ملاحظات
   - `created_at`, `updated_at`

## 🚀 خطوات الإعداد والتشغيل

### 1. تشغيل المايجريشن

```bash
cd C:\xampp\htdocs\jawda-medical
php artisan migrate
```

سيتم إنشاء الجداول الأربعة في قاعدة البيانات.

### 2. التحقق من الجداول

يمكنك التحقق من إنشاء الجداول عبر:

```bash
php artisan tinker
>>> \App\Models\Ward::count()
>>> \App\Models\Room::count()
>>> \App\Models\Bed::count()
>>> \App\Models\Admission::count()
```

## 📖 دليل الاستخدام التفصيلي

### الجزء الأول: إعداد البنية التحتية

#### 1. إضافة الأقسام (Wards)

**الوصول:** الإعدادات → الأقسام (`/settings/wards`)

**خطوات إضافة قسم جديد:**

1. اضغط على زر "إضافة قسم جديد"
2. املأ البيانات:
   - **اسم القسم**: اسم واضح للقسم (مثال: "قسم الجراحة العامة")
   - **الوصف**: وصف تفصيلي للقسم (اختياري)
   - **الحالة**: حدد إذا كان القسم نشطاً أم لا
3. اضغط "إضافة"

**مثال:**
- اسم القسم: قسم الجراحة العامة
- الوصف: قسم متخصص في العمليات الجراحية العامة
- الحالة: ✓ نشط

**ملاحظات:**
- لا يمكن حذف قسم إذا كان يحتوي على غرف
- يمكن تعديل بيانات القسم في أي وقت
- يمكن البحث عن الأقسام باستخدام حقل البحث

#### 2. إضافة الغرف (Rooms)

**الوصول:** الإعدادات → الغرف (`/settings/rooms`)

**خطوات إضافة غرفة جديدة:**

1. اضغط على زر "إضافة غرفة جديدة"
2. املأ البيانات:
   - **القسم**: اختر القسم الذي تنتمي إليه الغرفة
   - **رقم الغرفة**: رقم فريد للغرفة (مثال: "101", "A-205")
   - **نوع الغرفة**: نوع الغرفة (خاصة، شبه خاصة، عامة) - اختياري
   - **السعة**: عدد الأسرّة التي يمكن أن تحتويها الغرفة
   - **الحالة**: حدد إذا كانت الغرفة نشطة
3. اضغط "إضافة"

**مثال:**
- القسم: قسم الجراحة العامة
- رقم الغرفة: 101
- نوع الغرفة: خاصة
- السعة: 2
- الحالة: ✓ نشط

**ملاحظات:**
- لا يمكن حذف غرفة إذا كانت تحتوي على أسرّة
- يمكن تصفية الغرف حسب القسم
- السعة تحدد عدد الأسرّة التي يمكن إضافتها لاحقاً

#### 3. إضافة الأسرّة (Beds)

**الوصول:** الإعدادات → الأسرّة (`/settings/beds`)

**خطوات إضافة سرير جديد:**

1. اضغط على زر "إضافة سرير جديد"
2. املأ البيانات:
   - **الغرفة**: اختر الغرفة التي ينتمي إليها السرير
   - **رقم السرير**: رقم فريد للسرير (مثال: "1", "A", "سرير 1")
   - **الحالة**: 
     - **متاح**: السرير جاهز للاستخدام
     - **مشغول**: السرير مشغول حالياً (يتم تحديثه تلقائياً عند الإدخال)
     - **صيانة**: السرير غير متاح للصيانة
3. اضغط "إضافة"

**مثال:**
- الغرفة: 101 (قسم الجراحة العامة)
- رقم السرير: 1
- الحالة: متاح

**ملاحظات:**
- لا يمكن حذف سرير إذا كان مشغولاً بإقامة نشطة
- حالة السرير تتحدث تلقائياً عند إدخال/إخراج/نقل مريض
- يمكن تصفية الأسرّة حسب الغرفة أو القسم

### الجزء الثاني: إدارة الإقامات

#### 4. إضافة إقامة جديدة

**الوصول:** الإقامات → إضافة إقامة جديدة (`/admissions/new`)

**خطوات إضافة إقامة:**

1. **اختيار المريض:**
   - ابدأ بكتابة اسم المريض أو رقم هاتفه في حقل البحث
   - سيظهر قائمة بالمرضى المطابقين
   - اختر المريض من القائمة

2. **اختيار القسم:**
   - اختر القسم من القائمة المنسدلة
   - بعد اختيار القسم، ستظهر الغرف التابعة له تلقائياً

3. **اختيار الغرفة:**
   - اختر الغرفة من القائمة المنسدلة
   - بعد اختيار الغرفة، ستظهر الأسرّة المتاحة تلقائياً

4. **اختيار السرير:**
   - اختر السرير من القائمة المنسدلة
   - **ملاحظة مهمة**: تظهر فقط الأسرّة المتاحة (status = available)

5. **معلومات الإقامة:**
   - **تاريخ الإقامة**: تاريخ دخول المريض (افتراضي: اليوم)
   - **وقت الإقامة**: وقت دخول المريض (افتراضي: الوقت الحالي)
   - **نوع الإقامة**: نوع الإقامة (طوارئ، مجدولة، نقل) - اختياري
   - **سبب الإقامة**: سبب دخول المريض - اختياري
   - **التشخيص**: التشخيص الأولي - اختياري
   - **الطبيب**: الطبيب المسؤول - اختياري
   - **ملاحظات**: أي ملاحظات إضافية - اختياري

6. اضغط "إضافة"

**مثال كامل:**
```
المريض: أحمد محمد
القسم: قسم الجراحة العامة
الغرفة: 101
السرير: 1
تاريخ الإقامة: 2025-12-25
وقت الإقامة: 10:30
نوع الإقامة: طوارئ
سبب الإقامة: عملية استئصال الزائدة الدودية
التشخيص: التهاب الزائدة الدودية الحاد
الطبيب: د. محمد علي
ملاحظات: حالة مستقرة
```

**ما يحدث تلقائياً:**
- يتم تحديث حالة السرير من "متاح" إلى "مشغول"
- يتم ربط الإقامة بالمريض والطبيب والقسم والغرفة والسرير
- يتم تسجيل المستخدم الذي أنشأ الإقامة

#### 5. عرض قائمة الإقامات

**الوصول:** الإقامات (`/admissions`)

**الميزات المتاحة:**

1. **البحث:**
   - ابحث عن إقامة باستخدام اسم المريض أو رقم هاتفه
   - النتائج تظهر فوراً أثناء الكتابة

2. **التصفية:**
   - تصفية حسب الحالة:
     - **مقيم**: المرضى المقيمين حالياً
     - **مخرج**: المرضى الذين تم إخراجهم
     - **منقول**: المرضى الذين تم نقلهم

3. **عرض التفاصيل:**
   - اضغط على زر "عرض" لرؤية تفاصيل الإقامة الكاملة

**معلومات معروضة:**
- اسم المريض
- القسم
- الغرفة
- السرير
- تاريخ الإقامة
- الحالة (مع لون مميز)

#### 6. عرض تفاصيل الإقامة

**الوصول:** الإقامات → تفاصيل الإقامة (`/admissions/:id`)

**المعلومات المعروضة:**

1. **معلومات المريض:**
   - اسم المريض

2. **معلومات الإقامة:**
   - القسم
   - الغرفة
   - السرير
   - تاريخ ووقت الإقامة
   - تاريخ ووقت الإخراج (إن وجد)

3. **معلومات طبية:**
   - نوع الإقامة
   - سبب الإقامة
   - التشخيص
   - الطبيب المسؤول

4. **معلومات إضافية:**
   - ملاحظات
   - حالة الإقامة (مع لون مميز)

**الأزرار المتاحة (للمرضى المقيمين فقط):**
- **نقل**: لنقل المريض إلى قسم/غرفة/سرير آخر
- **إخراج**: لإخراج المريض من المستشفى

#### 7. إخراج مريض (Discharge)

**الوصول:** من صفحة تفاصيل الإقامة

**خطوات الإخراج:**

1. اضغط على زر "إخراج" (يظهر فقط للمرضى المقيمين)
2. ستظهر نافذة حوار
3. املأ البيانات:
   - **تاريخ الإخراج**: تاريخ إخراج المريض (افتراضي: اليوم)
   - **وقت الإخراج**: وقت إخراج المريض (افتراضي: الوقت الحالي)
   - **ملاحظات**: أي ملاحظات حول الإخراج (اختياري)
4. اضغط "إخراج"

**مثال:**
```
تاريخ الإخراج: 2025-12-28
وقت الإخراج: 14:00
ملاحظات: تم الإخراج بعد استكمال العلاج بنجاح
```

**ما يحدث تلقائياً:**
- يتم تحديث حالة الإقامة من "مقيم" إلى "مخرج"
- يتم تحديث حالة السرير من "مشغول" إلى "متاح"
- يتم حفظ تاريخ ووقت الإخراج

**ملاحظات:**
- لا يمكن إخراج مريض تم إخراجه مسبقاً
- بعد الإخراج، لا يمكن تعديل الإقامة إلا في حالات خاصة

#### 8. نقل مريض (Transfer)

**الوصول:** من صفحة تفاصيل الإقامة

**خطوات النقل:**

1. اضغط على زر "نقل" (يظهر فقط للمرضى المقيمين)
2. ستظهر نافذة حوار
3. اختر الموقع الجديد:
   - **القسم الجديد**: اختر القسم الجديد
   - **الغرفة الجديدة**: اختر الغرفة الجديدة (تظهر غرف القسم المختار)
   - **السرير الجديد**: اختر السرير الجديد (تظهر فقط الأسرّة المتاحة)
   - **ملاحظات**: أي ملاحظات حول النقل (اختياري)
4. اضغط "نقل"

**مثال:**
```
القسم الجديد: قسم العناية المركزة
الغرفة الجديدة: ICU-1
السرير الجديد: 1
ملاحظات: نقل بسبب تدهور الحالة الصحية
```

**ما يحدث تلقائياً:**
- يتم تحديث حالة الإقامة القديمة إلى "منقول"
- يتم تحديث حالة السرير القديم من "مشغول" إلى "متاح"
- يتم إنشاء إقامة جديدة في الموقع الجديد بحالة "مقيم"
- يتم تحديث حالة السرير الجديد من "متاح" إلى "مشغول"
- يتم ربط الإقامة الجديدة بالإقامة القديمة (نوع: transfer)

**ملاحظات:**
- لا يمكن نقل مريض تم إخراجه
- يجب التأكد من توفر السرير الجديد قبل النقل
- يمكن تتبع تاريخ النقل من خلال الإقامة القديمة

### الجزء الثالث: الإدارة والصيانة

#### 9. تعديل الأقسام/الغرف/الأسرّة

**خطوات التعديل:**

1. من صفحة القائمة، اضغط على أيقونة التعديل (✏️)
2. قم بتعديل البيانات المطلوبة
3. اضغط "تحديث"

**ملاحظات:**
- يمكن تعديل جميع البيانات عدا المعرف (ID)
- التعديلات تؤثر على الإقامات المستقبلية فقط
- الإقامات الحالية تحتفظ بالبيانات القديمة

#### 10. حذف الأقسام/الغرف/الأسرّة

**خطوات الحذف:**

1. من صفحة القائمة، اضغط على أيقونة الحذف (🗑️)
2. ستظهر نافذة تأكيد
3. اضغط "حذف" للتأكيد

**قواعد الحذف:**

- **لا يمكن حذف قسم** إذا كان يحتوي على غرف
- **لا يمكن حذف غرفة** إذا كانت تحتوي على أسرّة
- **لا يمكن حذف سرير** إذا كان مشغولاً بإقامة نشطة

**ملاحظات:**
- يجب حذف الأسرّة أولاً، ثم الغرف، ثم الأقسام
- أو يمكنك تعطيل العنصر بدلاً من حذفه (تغيير الحالة إلى غير نشط)

#### 11. البحث والتصفية

**البحث:**
- جميع صفحات القوائم تحتوي على حقل بحث
- البحث يعمل على الأسماء والأرقام
- النتائج تظهر فوراً أثناء الكتابة

**التصفية:**
- **الأقسام**: حسب الحالة (نشط/غير نشط)
- **الغرف**: حسب القسم والحالة
- **الأسرّة**: حسب الغرفة والقسم والحالة
- **الإقامات**: حسب الحالة والقسم والمريض

## 🔧 استكشاف الأخطاء وحلها

### مشكلة: لا تظهر الأسرّة المتاحة عند إضافة إقامة

**الحل:**
1. تأكد من أن السرير في حالة "متاح" (available)
2. تأكد من عدم وجود إقامة نشطة على السرير
3. تحقق من أن الغرفة نشطة
4. تحقق من أن القسم نشط

### مشكلة: لا يمكن حذف قسم/غرفة/سرير

**الحل:**
- تأكد من عدم وجود عناصر مرتبطة:
  - قسم: لا يحتوي على غرف
  - غرفة: لا تحتوي على أسرّة
  - سرير: غير مشغول بإقامة نشطة

### مشكلة: لا تظهر الغرف عند اختيار قسم

**الحل:**
1. تأكد من وجود غرف في القسم المختار
2. تحقق من أن الغرف نشطة
3. جرب تحديث الصفحة

### مشكلة: لا يمكن إخراج مريض

**الحل:**
1. تأكد من أن حالة الإقامة هي "مقيم" (admitted)
2. تحقق من أن المريض لم يتم إخراجه مسبقاً
3. تحقق من الصلاحيات

### مشكلة: لا يمكن نقل مريض

**الحل:**
1. تأكد من أن حالة الإقامة هي "مقيم"
2. تأكد من توفر سرير في الموقع الجديد
3. تحقق من أن السرير الجديد في حالة "متاح"

## 📊 أمثلة عملية

### مثال 1: إعداد قسم جديد كامل

```
1. إضافة قسم:
   - الاسم: قسم القلب
   - الوصف: قسم متخصص في أمراض القلب
   - الحالة: نشط

2. إضافة غرف:
   - الغرفة 201: سعة 2، نوع: خاصة
   - الغرفة 202: سعة 2، نوع: خاصة
   - الغرفة 203: سعة 4، نوع: عامة

3. إضافة أسرّة:
   - في الغرفة 201: سرير 1، سرير 2
   - في الغرفة 202: سرير 1، سرير 2
   - في الغرفة 203: سرير 1، سرير 2، سرير 3، سرير 4
```

### مثال 2: دورة حياة إقامة كاملة

```
1. إدخال مريض:
   - المريض: سارة أحمد
   - القسم: قسم القلب
   - الغرفة: 201
   - السرير: 1
   - التاريخ: 2025-12-25
   - السبب: فحص دوري

2. نقل المريض:
   - القسم الجديد: قسم العناية المركزة
   - الغرفة الجديدة: ICU-1
   - السرير الجديد: 1
   - السبب: تدهور الحالة

3. إخراج المريض:
   - التاريخ: 2025-12-30
   - السبب: تحسن الحالة واستكمال العلاج
```

### مثال 3: إدارة الأسرّة

```
1. سرير في صيانة:
   - الغرفة: 201
   - السرير: 2
   - الحالة: صيانة
   - ملاحظة: لا يظهر في قائمة الأسرّة المتاحة

2. بعد إصلاح السرير:
   - تغيير الحالة إلى "متاح"
   - يصبح متاحاً للإقامات الجديدة
```

## 🔐 الصلاحيات والأمان

### الصلاحيات المطلوبة

- **عرض الإقامات**: جميع المستخدمين المصرح لهم
- **إضافة إقامة**: المستخدمون المصرح لهم
- **تعديل إقامة**: المستخدمون المصرح لهم
- **إخراج مريض**: المستخدمون المصرح لهم
- **نقل مريض**: المستخدمون المصرح لهم
- **إدارة الأقسام/الغرف/الأسرّة**: المستخدمون المصرح لهم

### الأمان

- جميع العمليات تسجل المستخدم الذي قام بها
- لا يمكن حذف بيانات مرتبطة بإقامات
- التحقق من صحة البيانات في Backend و Frontend
- منع التلاعب بالحالات (مثلاً: إخراج مريض مخرج مسبقاً)

## 📱 واجهة المستخدم

### الألوان والدلالات

- **أخضر**: حالة "مقيم" أو "نشط" أو "متاح"
- **رمادي**: حالة "مخرج" أو "غير نشط"
- **أزرق**: حالة "منقول"
- **أحمر**: حالة "مشغول" أو "خطأ"
- **برتقالي**: حالة "صيانة"

### الأيقونات

- ✏️: تعديل
- 🗑️: حذف
- 👁️: عرض
- ➕: إضافة
- 🔍: بحث
- ⬅️: العودة

## 🔄 التكامل مع الأنظمة الأخرى

### التكامل مع نظام المرضى

- الإقامات مرتبطة بجدول `patients`
- يمكن البحث عن مريض من نظام الإقامات
- يمكن عرض تاريخ إقامات المريض

### التكامل مع نظام الأطباء

- الإقامات مرتبطة بجدول `doctors`
- يمكن تعيين طبيب لكل إقامة
- يمكن عرض إقامات الطبيب

### التكامل مع نظام المستخدمين

- جميع العمليات تسجل المستخدم الذي قام بها
- يمكن تتبع من أنشأ/عدّل/حذف أي إقامة

## 📈 التقارير والإحصائيات

### الإحصائيات المتاحة

1. **عدد الإقامات النشطة**: عدد المرضى المقيمين حالياً
2. **معدل الإشغال**: نسبة الأسرّة المشغولة
3. **معدل الإقامة**: متوسط مدة الإقامة
4. **إحصائيات الأقسام**: عدد الإقامات لكل قسم

### التقارير المستقبلية

- تقرير إشغال الأسرّة
- تقرير الإقامات حسب القسم
- تقرير مدة الإقامة
- تقرير الإخراج

## 🆘 الدعم والمساعدة

### في حالة وجود مشاكل

1. تحقق من سجلات الأخطاء في Laravel
2. تحقق من Console في المتصفح
3. تأكد من تشغيل المايجريشن
4. تأكد من صحة البيانات في قاعدة البيانات

### الأسئلة الشائعة

**س: هل يمكن نقل مريض إلى نفس السرير؟**
ج: لا، يجب اختيار سرير مختلف.

**س: هل يمكن إخراج مريض تم نقله؟**
ج: نعم، يمكن إخراج المريض من الإقامة الجديدة.

**س: ماذا يحدث للبيانات القديمة عند التعديل؟**
ج: البيانات القديمة محفوظة في الإقامات السابقة، التعديل يؤثر على الإقامات الجديدة فقط.

**س: هل يمكن حذف إقامة؟**
ج: لا، الإقامات لا يمكن حذفها، ولكن يمكن إخراج المريض.

## 📝 ملاحظات نهائية

- النظام مصمم ليكون سهل الاستخدام وآمناً
- جميع العمليات الحساسة تتطلب تأكيد
- البيانات محفوظة بشكل دائم ولا يمكن فقدانها
- النظام قابل للتوسع والتطوير

---

**آخر تحديث**: 25 ديسمبر 2025
**الإصدار**: 1.0.0

