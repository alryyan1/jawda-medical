# دليل اختبار نظام التنويم - Admission System Testing Guide

## 📋 جدول المحتويات

1. [متطلبات الاختبار](#متطلبات-الاختبار)
2. [إعداد البيئة](#إعداد-البيئة)
3. [اختبارات الوحدة (Unit Tests)](#اختبارات-الوحدة-unit-tests)
4. [اختبارات التكامل (Integration Tests)](#اختبارات-التكامل-integration-tests)
5. [اختبارات الواجهة (UI Tests)](#اختبارات-الواجهة-ui-tests)
6. [اختبارات الأداء (Performance Tests)](#اختبارات-الأداء-performance-tests)
7. [سيناريوهات الاختبار الشاملة](#سيناريوهات-الاختبار-الشاملة)
8. [قائمة التحقق (Checklist)](#قائمة-التحقق-checklist)

---

## 🔧 متطلبات الاختبار

### الأدوات المطلوبة

- ✅ PHP 8.1+ مع PHPUnit
- ✅ Laravel 10+
- ✅ MySQL/MariaDB
- ✅ Postman أو أي أداة لاختبار API
- ✅ متصفح حديث (Chrome, Firefox, Edge)
- ✅ Node.js 18+ (لاختبار Frontend)

### البيانات المطلوبة

قبل البدء بالاختبار، تأكد من وجود:
- ✅ مستخدمين بصلاحيات مختلفة
- ✅ أقسام (Wards) نشطة
- ✅ غرف (Rooms) نشطة
- ✅ أسرّة (Beds) متاحة
- ✅ مرضى (Patients) مسجلين

---

## 🛠️ إعداد البيئة

### 1. إعداد قاعدة البيانات للاختبار

```bash
# إنشاء قاعدة بيانات للاختبار
php artisan migrate:fresh --seed

# أو استخدام قاعدة بيانات منفصلة للاختبار
# في .env.testing:
DB_DATABASE=jawda_medical_test
```

### 2. تشغيل الخادم

```bash
# Backend (Laravel)
php artisan serve

# Frontend (Vite)
cd ../jawda-frontend
npm run dev
```

### 3. الحصول على Token للاختبار

```bash
# تسجيل الدخول والحصول على token
POST /api/login
{
  "email": "admin@example.com",
  "password": "password"
}

# استخدم الـ token في header:
Authorization: Bearer {token}
```

---

## 🧪 اختبارات الوحدة (Unit Tests)

### 1. اختبار احتساب مدة الإقامة

**الملف**: `tests/Unit/AdmissionStayDaysTest.php`

```php
<?php

namespace Tests\Unit;

use Tests\TestCase;
use App\Models\Admission;
use Carbon\Carbon;

class AdmissionStayDaysTest extends TestCase
{
    /** @test */
    public function it_calculates_morning_period_stay_days()
    {
        $admission = Admission::factory()->create([
            'admission_date' => '2026-02-03',
            'admission_time' => '08:00:00',
            'discharge_date' => '2026-02-04',
            'discharge_time' => '10:00:00',
        ]);

        // 26 hours = 2 days (rounded up)
        $this->assertEquals(2, $admission->days_admitted);
    }

    /** @test */
    public function it_calculates_evening_period_stay_days()
    {
        $admission = Admission::factory()->create([
            'admission_date' => '2026-02-03',
            'admission_time' => '14:00:00',
            'discharge_date' => '2026-02-04',
            'discharge_time' => '06:00:00',
        ]);

        // Should be 1 full day
        $this->assertEquals(1, $admission->days_admitted);
    }

    /** @test */
    public function it_calculates_default_period_stay_days()
    {
        $admission = Admission::factory()->create([
            'admission_date' => '2026-02-03',
            'admission_time' => '06:30:00',
            'discharge_date' => '2026-02-05',
            'discharge_time' => '06:30:00',
        ]);

        // (5 - 3) + 1 = 3 days
        $this->assertEquals(3, $admission->days_admitted);
    }
}
```

**تشغيل الاختبار**:
```bash
php artisan test --filter AdmissionStayDaysTest
```

### 2. اختبار حساب الرصيد المطلوب

**الملف**: `tests/Unit/AdmissionBalanceTest.php`

```php
<?php

namespace Tests\Unit;

use Tests\TestCase;
use App\Models\Admission;
use App\Models\AdmissionTransaction;

class AdmissionBalanceTest extends TestCase
{
    /** @test */
    public function it_calculates_balance_correctly()
    {
        $admission = Admission::factory()->create();

        // Add charges (debits)
        AdmissionTransaction::factory()->create([
            'admission_id' => $admission->id,
            'type' => 'debit',
            'amount' => 500.00,
        ]);

        AdmissionTransaction::factory()->create([
            'admission_id' => $admission->id,
            'type' => 'debit',
            'amount' => 300.00,
        ]);

        // Add payments (credits)
        AdmissionTransaction::factory()->create([
            'admission_id' => $admission->id,
            'type' => 'credit',
            'amount' => 400.00,
        ]);

        // Balance = 800 - 400 = 400
        $balance = $admission->balance;
        $this->assertEquals(400.00, $balance);
    }
}
```

---

## 🔗 اختبارات التكامل (Integration Tests)

### 1. اختبار إنشاء تنويم جديد

**الملف**: `tests/Feature/AdmissionCreationTest.php`

```php
<?php

namespace Tests\Feature;

use Tests\TestCase;
use App\Models\User;
use App\Models\Patient;
use App\Models\Ward;
use App\Models\Room;
use App\Models\Bed;

class AdmissionCreationTest extends TestCase
{
    public function test_create_bed_based_admission()
    {
        $user = User::factory()->create();
        $patient = Patient::factory()->create();
        $ward = Ward::factory()->create();
        $room = Room::factory()->create(['ward_id' => $ward->id]);
        $bed = Bed::factory()->create([
            'room_id' => $room->id,
            'status' => 'available',
        ]);

        $response = $this->actingAs($user)->postJson('/api/admissions', [
            'patient_id' => $patient->id,
            'ward_id' => $ward->id,
            'room_id' => $room->id,
            'bed_id' => $bed->id,
            'booking_type' => 'bed',
            'admission_date' => '2026-02-03',
            'admission_time' => '10:30:00',
            'diagnosis' => 'Test diagnosis',
        ]);

        $response->assertStatus(201);
        $this->assertDatabaseHas('admissions', [
            'patient_id' => $patient->id,
            'bed_id' => $bed->id,
            'booking_type' => 'bed',
        ]);

        // Check bed status updated
        $this->assertDatabaseHas('beds', [
            'id' => $bed->id,
            'status' => 'occupied',
        ]);
    }

    public function test_create_room_based_admission()
    {
        $user = User::factory()->create();
        $patient = Patient::factory()->create();
        $ward = Ward::factory()->create();
        $room = Room::factory()->create(['ward_id' => $ward->id]);

        $response = $this->actingAs($user)->postJson('/api/admissions', [
            'patient_id' => $patient->id,
            'ward_id' => $ward->id,
            'room_id' => $room->id,
            'bed_id' => null,
            'booking_type' => 'room',
            'admission_date' => '2026-02-03',
            'admission_time' => '14:00:00',
            'diagnosis' => 'Test diagnosis',
        ]);

        $response->assertStatus(201);
        $this->assertDatabaseHas('admissions', [
            'patient_id' => $patient->id,
            'room_id' => $room->id,
            'bed_id' => null,
            'booking_type' => 'room',
        ]);
    }
}
```

### 2. اختبار كشف الحساب

**الملف**: `tests/Feature/AdmissionLedgerTest.php`

```php
<?php

namespace Tests\Feature;

use Tests\TestCase;
use App\Models\User;
use App\Models\Admission;
use App\Models\AdmissionTransaction;

class AdmissionLedgerTest extends TestCase
{
    public function test_get_admission_ledger()
    {
        $user = User::factory()->create();
        $admission = Admission::factory()->create();

        // Add transactions
        AdmissionTransaction::factory()->create([
            'admission_id' => $admission->id,
            'type' => 'debit',
            'amount' => 500.00,
            'description' => 'Room charges',
        ]);

        AdmissionTransaction::factory()->create([
            'admission_id' => $admission->id,
            'type' => 'credit',
            'amount' => 300.00,
            'description' => 'Payment',
        ]);

        $response = $this->actingAs($user)->getJson(
            "/api/admissions/{$admission->id}/ledger"
        );

        $response->assertStatus(200);
        $response->assertJsonStructure([
            'admission_id',
            'patient',
            'summary' => [
                'total_debits',
                'total_credits',
                'balance',
            ],
            'entries' => [
                '*' => [
                    'id',
                    'type',
                    'amount',
                    'description',
                    'balance_after',
                ],
            ],
        ]);

        $data = $response->json();
        $this->assertEquals(500.00, $data['summary']['total_debits']);
        $this->assertEquals(300.00, $data['summary']['total_credits']);
        $this->assertEquals(200.00, $data['summary']['balance']);
    }

    public function test_add_transaction()
    {
        $user = User::factory()->create();
        $admission = Admission::factory()->create();

        $response = $this->actingAs($user)->postJson(
            "/api/admissions/{$admission->id}/transactions",
            [
                'type' => 'debit',
                'amount' => 100.00,
                'description' => 'Test charge',
                'reference_type' => 'charge',
            ]
        );

        $response->assertStatus(201);
        $this->assertDatabaseHas('admission_transactions', [
            'admission_id' => $admission->id,
            'type' => 'debit',
            'amount' => 100.00,
        ]);
    }
}
```

---

## 🖥️ اختبارات الواجهة (UI Tests)

### 1. اختبار إنشاء تنويم جديد

**الخطوات**:
1. افتح المتصفح وانتقل إلى `/admissions/new`
2. املأ النموذج:
   - اختر مريض
   - اختر قسم
   - اختر غرفة
   - **اختر نوع الحجز**: "حجز عن طريق السرير"
   - اختر سرير
   - أدخل تاريخ ووقت الدخول
   - أدخل التشخيص
3. اضغط "حفظ"
4. **التحقق**:
   - ✅ يجب أن يظهر رسالة نجاح
   - ✅ يجب أن يتم توجيهك إلى صفحة التفاصيل
   - ✅ يجب أن يكون السرير "مشغول" في لوحة الغرف

### 2. اختبار حجز غرفة بالكامل

**الخطوات**:
1. افتح `/admissions/new`
2. املأ النموذج:
   - اختر مريض
   - اختر قسم
   - اختر غرفة (VIP مثلاً)
   - **اختر نوع الحجز**: "حجز عن طريق الغرفة"
   - **لا تختار سرير** (يجب أن يكون الحقل معطل)
   - أدخل باقي البيانات
3. اضغط "حفظ"
4. **التحقق**:
   - ✅ يجب أن يظهر رسالة نجاح
   - ✅ في لوحة الغرف (`/admissions/dashboard`):
     - ✅ يجب أن تظهر الغرفة بحدود حمراء
     - ✅ يجب أن تظهر "الغرفة محجوزة بالكامل"
     - ✅ يجب أن تظهر معلومات المريض
     - ✅ يجب أن تكون نسبة الإشغال 100%

### 3. اختبار كشف الحساب

**الخطوات**:
1. افتح صفحة تفاصيل تنويم (`/admissions/{id}`)
2. اضغط على تبويب "كشف الحساب"
3. **التحقق من العرض**:
   - ✅ يجب أن تظهر بطاقات الملخص (المستحقات، المدفوعات، الرصيد)
   - ✅ يجب أن يظهر جدول المعاملات
   - ✅ يجب أن تظهر الأزرار (إضافة دفعة، إضافة خصم، إضافة رسوم)

4. **اختبار إضافة دفعة**:
   - اضغط "إضافة دفعة"
   - أدخل المبلغ (مثلاً: 1000)
   - اختر نوع الدفعة (نقدي/بنكي)
   - أدخل الوصف
   - اضغط "إضافة"
   - **التحقق**:
     - ✅ يجب أن تظهر رسالة نجاح
     - ✅ يجب أن يظهر في الجدول كـ "دفعة" (أخضر)
     - ✅ يجب أن يتحدث الرصيد المطلوب

5. **اختبار إضافة رسوم**:
   - اضغط "إضافة رسوم"
   - أدخل المبلغ (مثلاً: 500)
   - أدخل الوصف (مثلاً: "رسوم علاج")
   - اضغط "إضافة"
   - **التحقق**:
     - ✅ يجب أن تظهر كـ "رسوم" (أحمر)
     - ✅ يجب أن يتحدث الرصيد المطلوب

6. **اختبار احتساب رسوم الإقامة**:
   - اضغط "احتساب الإقامة"
   - **التحقق**:
     - ✅ يجب أن تظهر رسالة نجاح
     - ✅ يجب أن تظهر معاملة "رسوم إقامة" (أزرق)
     - ✅ المبلغ = عدد الأيام × سعر اليوم

### 4. اختبار ملف الإقامة

**الخطوات**:
1. افتح صفحة تفاصيل تنويم
2. اضغط على تبويب "ملف الإقامة"
3. **اختبار إضافة بيانات علاج**:
   - اختر "بيانات العلاج"
   - اضغط "إضافة بيانات علاج"
   - املأ النموذج
   - **التحقق**: يجب أن تظهر في القائمة

4. **اختبار إضافة جرعة**:
   - اختر "الجرعات"
   - اضغط "إضافة جرعة"
   - املأ النموذج (اسم الدواء مطلوب)
   - **التحقق**: يجب أن تظهر في القائمة

5. **اختبار إضافة مهمة تمريضية**:
   - اختر "المهام التمريضية"
   - اضغط "إضافة مهمة تمريضية"
   - املأ النموذج
   - **التحقق**: يجب أن تظهر في القائمة

---

## ⚡ اختبارات الأداء (Performance Tests)

### 1. اختبار تحميل قائمة التنويمات

```bash
# استخدام Apache Bench
ab -n 100 -c 10 -H "Authorization: Bearer {token}" \
  http://localhost/api/admissions?per_page=50
```

**المتوقع**:
- ✅ وقت الاستجابة < 500ms
- ✅ لا توجد أخطاء

### 2. اختبار كشف الحساب مع معاملات كثيرة

**السيناريو**:
- تنويم مع 1000 معاملة

**التحقق**:
- ✅ وقت الاستجابة < 1s
- ✅ البيانات صحيحة

---

## 📝 سيناريوهات الاختبار الشاملة

### السيناريو 1: دورة حياة تنويم كاملة

1. **إنشاء تنويم** (حجز بالسرير)
   - ✅ إنشاء تنويم جديد
   - ✅ التحقق من حالة السرير

2. **إضافة بيانات طبية**
   - ✅ إضافة بيانات علاج
   - ✅ إضافة جرعات
   - ✅ إضافة مهام تمريضية

3. **إدارة المالية**
   - ✅ احتساب رسوم الإقامة
   - ✅ إضافة دفعات
   - ✅ إضافة رسوم إضافية
   - ✅ إضافة خصومات

4. **نقل المريض**
   - ✅ نقل إلى غرفة أخرى
   - ✅ التحقق من تحديث حالة السرير القديم
   - ✅ التحقق من تحديث حالة السرير الجديد

5. **إخراج المريض**
   - ✅ إخراج المريض
   - ✅ التحقق من تحديث حالة السرير
   - ✅ التحقق من تحديث حالة التنويم

### السيناريو 2: حجز غرفة VIP

1. **إنشاء تنويم** (حجز بالغرفة)
   - ✅ إنشاء تنويم بدون سرير محدد
   - ✅ التحقق من عرض الغرفة كمشغولة بالكامل

2. **التحقق من العرض البصري**
   - ✅ في لوحة الغرف، يجب أن تظهر:
     - حدود حمراء
     - "الغرفة محجوزة بالكامل"
     - معلومات المريض
     - نسبة إشغال 100%

3. **إدارة المالية**
   - ✅ احتساب رسوم الإقامة
   - ✅ إضافة دفعات

4. **إخراج المريض**
   - ✅ إخراج المريض
   - ✅ التحقق من أن الغرفة أصبحت متاحة

### السيناريو 3: احتساب مدة الإقامة

**اختبار الفترة الصباحية**:
- دخول: `2026-02-03 08:00:00`
- خروج: `2026-02-04 10:00:00`
- **المتوقع**: 2 يوم

**اختبار الفترة المسائية**:
- دخول: `2026-02-03 14:00:00`
- خروج: `2026-02-04 06:00:00`
- **المتوقع**: 1 يوم

**اختبار الفترة الافتراضية**:
- دخول: `2026-02-03 06:30:00`
- خروج: `2026-02-05 06:30:00`
- **المتوقع**: 3 أيام

### السيناريو 4: كشف الحساب المعقد

1. **إضافة معاملات متعددة**:
   - رسوم إقامة: 1000
   - رسوم خدمة: 500
   - رسوم فحص: 200
   - دفعة نقدية: 800
   - دفعة بنكية: 500
   - خصم: 100

2. **التحقق من الحسابات**:
   - إجمالي المستحقات: 1700 (1000 + 500 + 200)
   - إجمالي المدفوعات: 1300 (800 + 500)
   - الرصيد المطلوب: 400 (1700 - 1300)

3. **التحقق من الرصيد المتراكم**:
   - يجب أن يكون الرصيد المتراكم صحيحاً لكل معاملة

---

## ✅ قائمة التحقق (Checklist)

### الوظائف الأساسية

- [ ] إنشاء تنويم (حجز بالسرير)
- [ ] إنشاء تنويم (حجز بالغرفة)
- [ ] عرض قائمة التنويمات
- [ ] عرض تفاصيل التنويم
- [ ] تحديث التنويم
- [ ] نقل مريض
- [ ] إخراج مريض

### احتساب مدة الإقامة

- [ ] الفترة الصباحية (7:00 - 12:00)
- [ ] الفترة المسائية (1:00 - 6:00)
- [ ] الفترة الافتراضية (6:00 - 7:00)
- [ ] عرض عدد الأيام في كشف الحساب
- [ ] عرض شرح طريقة الحساب

### كشف الحساب

- [ ] عرض كشف الحساب
- [ ] احتساب رسوم الإقامة
- [ ] إضافة دفعة نقدية
- [ ] إضافة دفعة بنكية
- [ ] إضافة خصم
- [ ] إضافة رسوم يدوية
- [ ] حذف معاملة
- [ ] حساب الرصيد المطلوب
- [ ] عرض الرصيد المتراكم

### ملف الإقامة

- [ ] إضافة بيانات علاج
- [ ] تحديث بيانات علاج
- [ ] حذف بيانات علاج
- [ ] إضافة جرعة
- [ ] تحديث جرعة
- [ ] حذف جرعة
- [ ] إضافة مهمة تمريضية
- [ ] تحديث مهمة تمريضية
- [ ] حذف مهمة تمريضية

### العرض البصري

- [ ] عرض لوحة الغرف
- [ ] عرض حالة الغرف (متاح/مشغول)
- [ ] عرض حالة الأسرّة
- [ ] عرض الغرف المحجوزة بالكامل
- [ ] عرض معلومات المريض على الغرفة
- [ ] عرض معلومات المريض على السرير

### الأمان

- [ ] التحقق من Authentication
- [ ] التحقق من Authorization
- [ ] التحقق من Validation
- [ ] منع الوصول غير المصرح به

### الأداء

- [ ] وقت تحميل قائمة التنويمات < 500ms
- [ ] وقت تحميل كشف الحساب < 1s
- [ ] وقت تحميل لوحة الغرف < 1s

---

## 🐛 تقارير الأخطاء

عند اكتشاف خطأ، يرجى توثيقه بالشكل التالي:

```markdown
### وصف الخطأ
[وصف واضح للخطأ]

### خطوات إعادة الخطأ
1. ...
2. ...
3. ...

### النتيجة المتوقعة
[ما كان يجب أن يحدث]

### النتيجة الفعلية
[ما حدث فعلاً]

### معلومات إضافية
- المتصفح: [Chrome/Firefox/Edge]
- الإصدار: [رقم الإصدار]
- نظام التشغيل: [Windows/Mac/Linux]
- لقطة شاشة: [إن أمكن]
```

---

## 📞 الدعم الفني

لأي استفسارات أو مشاكل تقنية أثناء الاختبار:
- 📧 البريد الإلكتروني: support@example.com
- 📱 الهاتف: +1234567890

---

**آخر تحديث**: فبراير 2026

**الإصدار**: 1.0.0
